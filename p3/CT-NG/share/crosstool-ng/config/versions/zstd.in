#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config ZSTD_DIR_NAME
    string
    default "zstd"



config ZSTD_PKG_NAME
    string
    default "zstd"

choice
    bool "Source of zstd"

config ZSTD_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config ZSTD_SRC_DEVEL
    bool "Vendor/custom repository"
    help
      Check out from a repository.
      Default is the vendor repository at https://github.com/facebook/zstd.git

config ZSTD_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

endchoice

if ZSTD_SRC_DEVEL

choice
    bool "VCS type"
    default ZSTD_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for zstd.

config ZSTD_DEVEL_VCS_git
    bool "Git"

config ZSTD_DEVEL_VCS_svn
    bool "Subversion"

config ZSTD_DEVEL_VCS_hg
    bool "Mercurial"

config ZSTD_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config ZSTD_DEVEL_VCS
    string
    default "git" if ZSTD_DEVEL_VCS_git
    default "svn" if ZSTD_DEVEL_VCS_svn
    default "hg" if ZSTD_DEVEL_VCS_hg
    default "cvs" if ZSTD_DEVEL_VCS_cvs

config ZSTD_DEVEL_URL
    string "Repository URL"
    default "https://github.com/facebook/zstd.git"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config ZSTD_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config ZSTD_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config ZSTD_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config ZSTD_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

if ZSTD_SRC_CUSTOM

config ZSTD_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

if EXPERIMENTAL
choice
    bool "zstd patches origin"
    default ZSTD_PATCH_GLOBAL

config ZSTD_PATCH_GLOBAL
    bool "Per global policy"

config ZSTD_PATCH_BUNDLED
    bool "Bundled only"

config ZSTD_PATCH_LOCAL
    bool "Local only"
    select PATCH_USE_LOCAL

config ZSTD_PATCH_BUNDLED_LOCAL
    bool "Bundled, then local"
    select PATCH_USE_LOCAL

config ZSTD_PATCH_LOCAL_BUNDLED
    bool "Local, then bundled"
    select PATCH_USE_LOCAL

config ZSTD_PATCH_NONE
    bool "None"

endchoice
endif

config ZSTD_PATCH_ORDER
    string
    default "bundled" if ZSTD_PATCH_BUNDLED
    default "local" if ZSTD_PATCH_LOCAL
    default "bundled,local" if ZSTD_PATCH_BUNDLED_LOCAL
    default "local,bundled" if ZSTD_PATCH_LOCAL_BUNDLED
    default "none" if ZSTD_PATCH_NONE
    default "global"

choice
    bool "Version of zstd"
    help
      For a released version, select the version of zstd to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building zstd.

config ZSTD_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on ZSTD_SRC_DEVEL || ZSTD_SRC_CUSTOM

config ZSTD_V_1_5_5
    bool "1.5.5"

config ZSTD_V_1_5_2
    bool "1.5.2"

config ZSTD_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on ZSTD_SRC_DEVEL || ZSTD_SRC_CUSTOM

endchoice

# A flag indicating that no valid selections exist for the version.
# This may happen if there are conflicting requirements set by
# the host system and/or other packages.
config ZSTD_NO_VERSIONS
    bool
    select INVALID_CONFIGURATION
    default n if ZSTD_VERY_NEW
    default n if ZSTD_V_1_5_5
    default n if ZSTD_V_1_5_2
    default n if ZSTD_VERY_OLD
    default y

if ZSTD_NO_VERSIONS
comment "WARNING!"
comment "|  The zstd package has no viable versions"
comment "|  due to conflicting constraints."
endif



config ZSTD_VERSION
    string
    default "1.5.5" if ZSTD_V_1_5_5
    default "1.5.2" if ZSTD_V_1_5_2
    default "new" if ZSTD_VERY_NEW
    default "old" if ZSTD_VERY_OLD
    default "unknown"

config ZSTD_MIRRORS
    string
    default "https://github.com/facebook/zstd/releases/download/v${CT_ZSTD_VERSION} https://www.zstd.net/"

config ZSTD_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config ZSTD_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config ZSTD_ARCHIVE_FORMATS
    string
    default ".tar.gz"

config ZSTD_SIGNATURE_FORMAT
    string
    default "packed/.sig"




